import React from "react";
import ReactDOM from "react-dom/client";
import TaskPaneView from "./taskpane/taskpaneview";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <TaskPaneView />
  </React.StrictMode>
);
